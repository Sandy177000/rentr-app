import React from 'react';
import NewItemForm from '../src/components/NewItemForm';

const NewItemScreen = () => {
  return (
      <NewItemForm setVisible={() => {}}  fullScreen={true}/>
  );
};

export default NewItemScreen;
